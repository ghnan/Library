@model IEnumerable<MyLibrary.Model.Entities.Borrowing>
@{
    ViewBag.Title = "BorrowingInfo";
}

<div class="jumbotron text-center" style="margin-bottom:0">
    <h1>这是借阅信息页面</h1>
</div>

<table class="table table-bordered" style="margin-top:50px;">
    <tr class="active">
        <th>
            #
        </th>
        <th>
            学生姓名
        </th>
        <th>
            借阅书籍名称
        </th>
        <th>
            借阅日期
        </th>
        <th>
            归还日期
        </th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BorrowingID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date_Return)
            </td>
        </tr>
    }
</table>

<a href="~/Admin/DoAdmin/AdminMain">返回管理员页面</a>